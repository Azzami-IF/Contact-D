import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'id.ac.uts.contactapp', // Diperbarui agar lebih profesional untuk UTS
  appName: 'ContactApp',
  webDir: 'www'
};

export default config;
